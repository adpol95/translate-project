/** @type {import('next').NextConfig} */
const nextConfig = {
    // i18n: {
    //     locales: ['en', 'fr', 'ru'],
    //     defaultLocale: 'en',
    //     localeDetection: true,
    // },
};

export default nextConfig;
